This application is written in PHP only
To run it, you need to put the folder in htdocs or www, run the private server, and then type the following link in the browser
"http://localhost:80/folder_name"
